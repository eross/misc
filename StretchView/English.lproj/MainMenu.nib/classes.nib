{
    IBClasses = (
        {
            ACTIONS = {fade = id; open = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {slider = NSSlider; stretchView = StretchView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = StretchView; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}